package com.eomcs.pms.handler;
import java.eomcs.pms.domain.
public class MemberList {
  static final int DEFAULT_CAPACITY = 100;

  Member[] list = new Member[DEFAULT_CAPACITY ]; // list로 이름을 바꾼다.
  int size = 0;

}
